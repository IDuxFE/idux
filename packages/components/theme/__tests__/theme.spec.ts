// import { MountingOptions, mount } from '@vue/test-utils'

// import { renderWork } from '@tests'

// import Theme from '../src/Theme'
// import { ThemeProps } from '../src/types'

describe.skip('Theme', () => {
  // const ThemeMount = (options?: MountingOptions<Partial<ThemeProps>>) =>
  //   mount(Theme, { ...(options as MountingOptions<ThemeProps>) })
  // renderWork<ThemeProps>(Theme, {
  //   props: {},
  // })
  // test('xxx work', async () => {
  //   const wrapper = ThemeMount({ props: { xxx: 'Xxx' } })
  //   expect(wrapper.classes()).toContain('ix-Xxx')
  //   await wrapper.setProps({ xxx: 'Yyy' })
  //   expect(wrapper.classes()).toContain('ix-Yyy')
  // })
})
